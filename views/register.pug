doctype html
html(lang='en')
  head
    title Sign Up - FSE Chat Room
    link(rel="stylesheet", href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.0-beta1/dist/css/bootstrap.min.css", integrity="sha384-giJF6kkoqNQ00vy+HMDP7azOuL0xtbfIcaT9wjKHr8RbDVddVHyTfAAsrekwKmP1", crossorigin="anonymous")
    link(rel="stylesheet", href="/stylesheets/style.css")
    style.
      body { height: 100%; display: flex; padding-top: 80px; align-items: center; }

  body
    .container(class="text-center")
      h4(class="mb-3 fw-normal") Welcome to FSE Chat Room!

      form(class="form-signin" method="POST" action="/users/register")
        h3(class="mb-3 fw-normal") Sign up

        label(for="username" class="visually-hidden") Username
        input(type="text" name="username" id="input-username" class="form-control" placeholder="Username" required autofocus)

        label(for="password" class="visually-hidden") Password
        input(type="password" name="password" id="input-password" class="form-control" placeholder="Password" minlength=6 maxlength=18 required)

        #error
          if error
            p.text-danger The username already exists.
            
        span Registered user? 
          a(href="/users/login") Login!

        button(type="submit" class="w-100 btn btn-lg btn-success" id="enter-btn") Submit
